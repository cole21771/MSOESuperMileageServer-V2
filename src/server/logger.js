module.exports = (winston) => {
  this.logPath = __dirname + '/test/';

  this.logData = (data) => {
    //console.log('data', data);
  };

  this.logLocation = (location) => {

  };

  return this;
};
